CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET(UNIT_NAME TestLC)

PROJECT(${UNIT_NAME} CXX)

get_filename_component(NC_MAIN_DIRECTORY ../../../ REALPATH)

INCLUDE(${NC_MAIN_DIRECTORY}/.BuildFiles/CommonSNC.cmake)

IF(${CMAKE_SYSTEM_NAME} MATCHES "Windows")
	GroupTests(LC)
ENDIF(${CMAKE_SYSTEM_NAME} MATCHES "Windows")

